import React from "react"
import PropTypes from "prop-types"
import IconSettings from 'images/IconSettings.svg'


class A_IconSettings extends React.Component {
  render (src) {
    return (
      <div>
        <img src={IconSettings} />
      </div>
    );
  }
}

export default A_IconSettings
