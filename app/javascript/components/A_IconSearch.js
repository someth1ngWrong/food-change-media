import React from "react"
import PropTypes from "prop-types"
import IconSearch from 'images/IconSearch.svg'


class A_IconSearch extends React.Component {
  render (src) {
    return (
      <div>
        <img src={IconSearch} />
      </div>
    );
  }
}

export default A_IconSearch
